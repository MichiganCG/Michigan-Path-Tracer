# This CMakeList.txt file will get populated once we add more source files to the project!

# add_subdirectory(Folder) # Use this to include a subdirectory

# Use the following to add source files
# target_sources(mpt PRIVATE
#         Scene.cpp
# )

add_subdirectory(Scene)